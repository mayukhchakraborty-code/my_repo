# Python virtual environments — covers root-level and all task-level .venv/
venv/
.venv/
env/
__pycache__/
*.pyc

# macOS metadata
.DS_Store

# -----------------------------------------------------------------
# viscosity/
# Large raw-data backups — kept locally on disk, never pushed
viscosity/ZN-*.json
viscosity/ZN-*.zip
# Generated output — produced by ingestion_viscosity.py
viscosity/output_reports/

# -----------------------------------------------------------------
# conversion_mt/
# Input/output Excel data files — not source code
conversion_mt/*.xlsx
conversion_mt/*.xls

# -----------------------------------------------------------------
# onboarding/
# Add any large data files here if needed in future
# onboarding/*.csv — intentionally NOT ignored (viscosity_lookup is tiny reference data)

# -----------------------------------------------------------------
# Template for new task folders — copy this block when adding a new task:
# new_task/
# new_task/output/
# new_task/input/*.json
# new_task/input/*.xlsx
